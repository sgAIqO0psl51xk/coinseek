[project]
name = "xkl1s"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.11.11",
    "black>=24.10.0",
    "flake8-pyproject>=1.2.3",
    "mypy>=1.14.1",
    "requests>=2.32.3",
]

[tool.mypy]
exclude = ".venv"

[tool.black]
line-length = 120
exclude = '.venv'

[tool.flake8]
max-line-length = 120
exclude = '.venv'
